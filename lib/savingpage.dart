import 'package:flutter/material.dart';

class Savingpage extends StatelessWidget {
  const Savingpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: Center(child: Text("sfjksfdjksfdjkfds")),
    );
  }
}